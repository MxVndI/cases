REDIS_URL=redis://redis:6379/0
MONGODB_URL=mongodb://mongodb:27017

# === Frontend / Auth ===
# Public URL where the frontend is accessible (used by auth service for OAuth redirects and cookie settings)
FRONTEND_URL=http://YOUR_SERVER_IP
# Leave empty for host-only cookies (recommended). Set to your domain if using a real domain name.
COOKIE_DOMAIN=
# API base URL seen by the browser (leave empty to auto-detect from window.location)
VITE_API_BASE_URL=
# Public URL of RustFS/S3 storage (used for image URLs returned to browser)
S3_PUBLIC_URL=http://YOUR_SERVER_IP:9000

MONGO_EXPRESS_USER=secret
MONGO_EXPRESS_PASSWORD=secret
REDIS_COMMANDER_HTTP_USER=secret
REDIS_COMMANDER_HTTP_PASSWORD=secret
AML_KEK_B64=yfUjB1985BhQj99E3N6lr0K98GMGxdsUWIz5Ln25i6U=
CSRF_SECRET=secret
# === Email / SMTP ===
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SMTP_USE_SSL=false
SMTP_STARTTLS=true
SMTP_TIMEOUT_S=10
EMAIL_ADDRESS=kingknife29@gmail.com
EMAIL_PASSWORD=ghkgceicszvnfrpk