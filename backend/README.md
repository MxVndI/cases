# Backend services

This repository contains multiple backend services (FastAPI) under the `backend/` folder.

Each service is executed directly (e.g. `python api.py` / `python main.py`) and shares a common dependency set defined in `pyproject.toml`.

